<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yaroslav.calcvault">

    <!-- Разрешения не требуются благодаря использованию Photo Picker -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Калькулятор"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalcVault">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CalcVault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Провайдер для безопасного открытия файлов (видео) из внутреннего хранилища -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>
</manifest>
